# PDF Module

This module renders invoices to A4 printable PDFs using Puppeteer.

## Responsibilities
* HTML+CSS print styling templates (A4 scale, margins, headers, totals)
* Puppeteer server-side browser execution
* Direct print page stylesheet configuration for browser printing (Ctrl + P)
